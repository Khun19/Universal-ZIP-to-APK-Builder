# Fixture layout

Add real fixture archives here for native Android, React + Vite, plain web, and existing Capacitor projects. Security fixtures should include an archive with `../escape.txt`, an absolute path, a symlink, and a highly compressed payload. Tests must assert rejection and must never execute fixture scripts on the host.